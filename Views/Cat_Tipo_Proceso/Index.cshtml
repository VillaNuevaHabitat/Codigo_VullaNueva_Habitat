@model IEnumerable<VillaNueva_Habitat.Models.Cat_Tipo_Proceso>

@{
    ViewBag.Title = "Index";
}

@{
    ViewBag.Title = "Index";
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        <strong>Error : </strong> @TempData["ErrorMessage"]
        <button type="button" class="close" data-dismiss="alert" aria-label="close">
            <span>&times;</span>
        </button>
    </div>
}
else if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        <strong>Success ! </strong> @TempData["SuccessMessage"]
        <button type="button" class="close" data-dismiss="alert" aria-label="close">
            <span>&times;</span>
        </button>
    </div>
}

else if (TempData["InfoMessage"] != null)
{
    <div class="alert alert-Info">
        <strong>Info ! </strong> @TempData["InfoMessage"]
        <button type="button" class="close" data-dismiss="alert" aria-label="close">
            <span>&times;</span>
        </button>
    </div>
}

<h2>Catálogo de Tipo de Proceso</h2>

<p>
    <a class="btn btn-primary mb-3" href="@Url.Action("Create","Cat_Tipo_Proceso")">Crear Nuevo Proceso</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdProceso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clave)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdProceso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clave)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.IdProceso }, new { @class = "btn btn-primary" }) 
                @Html.ActionLink("Detalles", "Details", new { id = item.IdProceso }, new { @class = "btn btn-success" }) 
                @Html.ActionLink("Eliminar", "Delete", new { id = item.IdProceso }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
